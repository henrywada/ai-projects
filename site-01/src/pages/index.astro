---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import Hr from "@/components/Hr.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index">
    <section id="hero" class="pt-8 pb-6">

     <h1 class="my-4 inline-block text-3xl font-bold sm:my-8 sm:text-5xl">
        中小企業の「人事」を、<br class="hidden sm:inline" />
        悩みの種から<span class="text-skin-accent">「最強の武器」</span>へ
      </h1>

      <p class="my-2 text-lg">
        <strong>経営者・人事マネージャーの「困った」を即解決。</strong><br />
        採用・定着・育成からDXまで、現場で使える<strong class="text-skin-accent">「答え」</strong>が見つかる知識の宝箱です。
      </p>
      
      <ul class="list-disc list-inside mt-4 space-y-1 text-skin-base opacity-90">
        <li><strong>人材確保・定着率</strong>の向上テクニック</li>
        <li><strong>評価制度・労務管理</strong>の運用ノウハウ</li>
        <li><strong>次世代育成・業務効率化</strong>の具体策</li>
      </ul>

    </section>

    <Hr />

    {
      featuredPosts.length > 0 && (
        <>
          <section id="featured" class="pt-12 pb-6">
            <h2 class="text-2xl font-semibold tracking-wide">おすすめ記事</h2>
            <ul>
              {featuredPosts.map(data => (
                <Card variant="h3" {...data} />
              ))}
            </ul>
          </section>
          {recentPosts.length > 0 && <Hr />}
        </>
      )
    }

    {
      recentPosts.length > 0 && (
        <section id="recent-posts" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">新着記事</h2>
          <ul>
            {recentPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" {...data} />
            )}
          </ul>
        </section>
      )
    }

    <div class="my-8 text-center">
      <LinkButton href="/posts/">
        すべての記事を見る
        <IconArrowRight class="inline-block rtl:-rotate-180" />
      </LinkButton>
    </div>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
